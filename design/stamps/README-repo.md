# design/stamps — 산별 스탬프 납품물 (2차, 2026-09-06)

디자이너 납품 원본. `preview/`·`standalone/`(PNG 미리보기)와 시트 SVG는 용량 때문에 뺐다.
코드에 들어가는 건 `scripts/build-stamp-art.mjs`가 `seasons/`·`motifs/`·`assignments.json`·`season-tokens.json`에서
만든 `src/data/stamp-art.json`뿐이다. 이 폴더를 바꾸면 스크립트를 다시 돌린다.
