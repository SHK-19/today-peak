# SESSIONS.md — Claude Code 세션 계획

각 세션은 검증 기준을 통과하면 끝. 프롬프트는 그대로 붙여넣는 용도.
세션 1~2는 사업자 검토와 무관하게 진행 가능. 세션 3a부터 토스 로그인이 필요하다.

---

## 세션 0 — 환경 (터미널)

이 세션은 Claude Code에 붙여넣을 프롬프트가 없다. 터미널 명령 + 콘솔 클릭만. 프롬프트 붙여넣기는 세션 1부터.

```bash
mkdir today-peak && cd today-peak && git init
# CLAUDE.md, SESSIONS.md를 이 폴더에 복사
claude plugin marketplace add toss/apps-in-toss-harness
claude plugin install ait@apps-in-toss -y
claude
```
Claude 안에서 `/mcp` → `apps-in-toss-console` 로그인 → `/ait:welcome`.

**지금 바로 콘솔에서 할 것 (사업자 검토 1~2일 + 정산 검토 2~3일, 병렬):**
- 사업자 정보 등록 (개인사업자)
- 정산 정보 등록 (예금주명은 통장 사본과 동일하게, 업태·업종은 텍스트로 검색)

**Supabase:** 프로젝트 하나 생성해두고 URL/anon key 준비. (세션 3에서 사용)

**데이터 (세션 1~3과 병렬, 사용자 작업):** 정상 좌표 + 등산로 입구 좌표. 공공데이터포털의 산림청/국토지리정보원 등산로 데이터에서
등산로 선형 끝점을 입구 후보로 뽑는 방식이 유력하나 데이터셋 적합성은 확인 필요. v0 출시 시점엔 서울 근교 명산 10곳 정도만 입구가 있어도 충분.

**검증:** `/ait:welcome` 정상 응답. 콘솔에 사업자 검토 요청됨.

---

## 세션 1 — 프로젝트 생성 + 순수 로직

```
이 폴더의 CLAUDE.md를 먼저 읽어줘.

"오늘 정상" 이름으로 앱인토스 WebView 미니앱을 만들어줘. 부제는 "100대 명산 정상 스탬프", 영문 이름은 "Today Peak".
생성 후 기본 템플릿의 스택(순수 JS / React 등)을 알려주고, 그 안에서 최소로 진행해.

그 다음 아래를 구현해줘. 시작 전에 [단계] → 검증: [방법] 형식으로 계획을 먼저 보여줘.

1. src/data/mountains.json — 스키마는 CLAUDE.md (trailheads 포함). 좌표는 지어내지 말고 샘플 3개만, 상단에 샘플 표시.
2. src/lib/geo.js — haversineMeters(lat1, lng1, lat2, lng2) 하나만. Node 테스트 포함
   (서울시청↔부산시청 ≈ 325km).
3. src/lib/verify.js — CLAUDE.md의 인증 판정을 순수 함수로:
   pickBestReading(readings), verifySummit(reading, mountain) → { ok, reason, distanceM }.
   Node 테스트: 정확도 낮음 / 반경 안 / 반경 밖 / 경계 / 고도 불일치 / altitudeAccuracy 나빠서 고도 무시.
SDK는 아직 쓰지 마. 이 세션은 UI 없이 로직만.
```

**검증:** `node` 테스트 전부 통과. 상수가 verify.js 상단에 모여 있음.

---

## 세션 2 — 홈 + 인증 UI (로그인 없이, 결과는 화면에만)

```
CLAUDE.md 읽고 진행해줘.

1. 홈: 권한 확인 → 위치 1회 조회 → 산 목록 거리순. 권한 거부 시 안내 화면.
2. 산 상세: 이름, 높이, "정상 인증하기" 버튼.
3. 버튼 → Accuracy.Highest로 최대 3회(5초 간격) 읽기 → pickBestReading → verifySummit → 결과 화면.
   reason별 문구: 정확도 낮음 / 정상까지 Nm / 고도 차이 / 성공(스탬프 획득).
   이 세션에서는 성공해도 저장하지 않는다. 화면에 distanceM, accuracy를 디버그로 표시해줘.
SDK 함수는 반드시 앱인토스 문서를 확인하고 써.
```

**검증:**
- 폰에서 홈이 열리고 가까운 산 순서가 맞음. 권한 거부 후 재진입해도 안 죽음.
- 샘플 산 좌표를 내 위치로 임시 교체 → 성공. 200m 떨어진 좌표 → "정상까지 Nm".

**이 세션 후 사용자가 직접:**
- 실제 산 1곳에서 인증 시도. 디버그 값(distanceM, accuracy, altitude 차이)을 기록해서 상수 조정 여부 판단.
  같이 판단할 미결 사항: 위치 읽기 조기 종료 규칙. 현재는 '1회차 오차 ≤ 50m면 남은 회차를 안 기다림'.
  경계(예: 105m)에서 아깝게 떨어지는 게 잦으면 '오차도 좋고 반경 안에도 들어왔을 때만 중단'으로 바꾼다. src/lib/location.ts 조건 한 줄.

### 실측 기록 (2026-09-04, 서울 도심 · elevationM=0인 테스트 장소 기준)

| 항목 | 실내(집 22층) | 야외(효창공원·운동장, 12회) |
|---|---|---|
| getCurrentLocation 소요 | 9~18ms | **3.3~10.0초** (평균 6.1초) |
| 프리페치 사용 시 소요 | 2ms | **1~3ms** |
| 채택 좌표 나이 | 1.1~10.7초 (OS 캐시) | 46~84ms (새 측정) |
| 정확도 | 8~12m | 3~32m (최악 32m) |
| altitudeAccuracy | ±30~68m | ±3~30m (50m 초과 0건) |
| 고도 | 50~54m | 33~59m |

판단:
- **프리페치는 유지한다.** 야외에서 6초를 아낀다. 실내 데이터만 보고 빼려던 판단은 틀렸다.
- `SUMMIT_RADIUS_M = 100` 검증됨. 203m 떨어진 두 지점이 12회 모두 동시 성공 없이 분리됐다.
- `MAX_HORIZONTAL_ACCURACY_M = 50` 유지. 최악이 32m라 여유 있다. `low_accuracy` 분기는 아직 한 번도 못 봤다.
- `MAX_READS = 3` 루프는 한 번도 2회차로 안 넘어갔다. 소요 6초는 루프가 아니라 호출 1회 자체의 비용이다.
- 고도 게이트는 야외에서 항상 켜진다(altitudeAccuracy가 50m를 안 넘음). 다만 테스트 장소가 elevationM=0이라 판정력은 미검증 — 산에서 확인한다.
- 실내에서는 getCurrentLocation이 최대 10.7초 묵은 OS 캐시를 즉시 돌려줬다. 만료 검사가 필요한 근거.

미해결로 남긴 것:
- `low_accuracy` — 실기기 검증 아직 못 함(최악 정확도가 32m라 발동 안 함). 산 나무 밑에서 확인.
- (해결) 권한 거부: OS 위치 서비스를 끄면 `getPermission`이 `allowed`를 주고 실제 조회에서 에러가 난다. 홈이 그 에러를 구분하지 않아 엉뚱한 화면("위치를 가져오지 못했어요")이 떴다. 2026-09-05 수정.
- (해결) **미니앱 단위 거부(`denied`)를 새 기기에서 처음 관측했다 (2026-09-05).** 미니앱을 처음 열 때 위치 허용을 거절하면, **토스 앱 자체의 위치 권한은 켜져 있는데도** 미니앱 권한만 거부 상태로 남는다. 사용자는 iOS 설정 → 토스에서 위치가 "허용"으로 보이기 때문에 어디를 고쳐야 하는지 알 수 없다 — 40·50대 사용자에게는 사실상 막다른 화면이다.
  → `ensureLocationPermission`이 `denied`에서도 `openPermissionDialog`를 다시 띄우도록 고쳤다(공식 예제가 `denied`에서 여는 방식이다). **권한 요청을 별도 버튼으로 빼지 않고 `정상 인증하기`·`산행 시작`·홈 진입 자체가 매번 다시 묻는다** — 거부한 뒤에도 버튼을 누르면 계속 물어본다. 위치 없이는 인증이 불가능한 기능이라 다른 길이 없다. 두 번째 거부부터는 "팝업이 뜨지 않으면 토스 앱 설정 > 권한에서 위치를 켜주세요"를 덧붙이기만 하고, 버튼 동작은 그대로 둔다.
- (해결) **401을 네트워크 오류처럼 안내하던 버그.** 세션이 만료되거나 서명키가 바뀌면 서버가 401을 주는데, 화면은 "인터넷 연결을 확인한 뒤 다시 저장해 주세요"를 띄웠다 — 14일 뒤 사용자가 영원히 "다시 저장"만 누르게 되는 막다른 길이었다. 이제 401이면 저장된 토큰을 지우고 로그인 화면으로 되돌린다(`expireSession` → `setSessionLostHandler`). 브라우저에서 위조 토큰으로 재현·확인했다.
- 같은 산의 봉우리를 따로 등록하면(백운대·인수봉·만경대 등 수백 m 간격) 반경 100m가 서로를 침범한다. v0은 산 하나에 정상 하나라 해당 없음. 봉우리별 스탬프를 검토할 때 다시 볼 것.

- (완료) 오프라인 테스트 결과: 비행기 모드에서 미니앱 메뉴 자체가 "인터넷이 없어요"로 막힘.
  → 오프라인 인증은 불가로 확정. 대기열·사진 인증 등 대체 수단은 만들지 않는다. 안내 문구와 재저장 버튼으로만 처리 (세션 3).

---

## 세션 3a — 토스 로그인 + login Edge Function (사업자 검토 완료 후)

```
CLAUDE.md의 "인증·서버 구조"를 먼저 읽고, 앱인토스 문서에서 토스 로그인(appLogin, generate-token, login-me, refresh-token)과
Supabase 연동 가이드를 확인한 뒤 계획을 [단계] → 검증 형식으로 보여줘.

1. 클라이언트: appLogin() → authorizationCode를 Edge Function login에 전달. 로그인 취소 시 앱이 죽지 않게.
2. Edge Function login: 토스 토큰 교환 → login-me → userKey → 우리 서명키로 세션 토큰 발급(만료 14일). 토스 토큰은 반환하지 않는다.
3. 클라이언트는 세션 토큰만 보관(메모리 + Storage API). 만료되면 다시 appLogin.
4. 비밀키는 Edge Function 환경변수. 클라이언트 번들에 비밀이 없는지 grep으로 확인해줘.
5. 비게임 출시 가이드의 토스 로그인 항목 4개를 확인하고 충족 여부를 알려줘.
```

**검증:** 폰에서 로그인 → 세션 토큰 수신 → Edge Function 로그에 userKey 찍힘. 로그인 취소 → 안내 화면. 번들에 비밀키 없음.

### 기록 (2026-09-05)

**mTLS가 세션 3a의 진짜 병목이다.** 사업자 검토·대표관리자·약관 동의는 이미 끝나 있었다
(콘솔에 clientId `gt4g477o...` 발급됨). 대신 토스 로그인 서버 API 3종이 전부 mTLS 클라이언트
인증서를 요구한다(문서 "서버 API 이용하기"). 인증서는 앱 검토를 먼저 받아야 신청할 수 있고,
앱 검토에는 스크린샷(세로 636×1048 3장 이상, 가로 1504×741 1장 이상)이 필요하다.
→ **순서가 바뀐다: 세션 4.5의 이미지 자산이 세션 3a의 실기기 검증보다 앞선다.**

**(해결) Supabase Edge Function에서 mTLS는 된다.** 폐기용 자체서명 인증서를 시크릿에 넣고
토스 서버까지 실제로 붙여봤다(2026-09-05). 응답:
`received fatal alert: UnknownCA` — 우리 인증서를 토스가 **받아보고** 모르는 CA라 거절한 것이다.
unstable API 에러도, 네트워크 차단도 아니었다. 런타임은 edge-runtime 1.74.4 (Deno 2.1.4).
→ CLAUDE.md의 아키텍처 결정(Supabase Edge Functions) 유지. 진짜 인증서만 꽂으면 된다.
확인 방법은 `login` 함수에 남겨뒀다: `curl '<fn-url>/login?handshake=1'`.

**(2026-09-05) 진짜 인증서로 핸드셰이크 성공.** 앱정보 검토 승인 → 콘솔에서 mTLS 인증서 발급
(이름 `todaypeakprod01`) → Edge Function 시크릿 등록 → 진단이 `handshake: ok, status: 200`.
토스가 돌려준 건 `OAUTH_ISSUE_TOKEN_ERROR / invalid_grant`인데, 진단이 일부러 보낸 가짜 인가
코드를 거절한 것이라 정상이다. TLS 상호 인증은 통과했고 요청이 로그인 API까지 닿았다.
남은 변수는 진짜 인가 코드뿐 — 실기기 로그인에서만 나온다.

인증서: 발급자 `CN=Toss appsintoss Root CA`, 주체 `CN=today-peak`, PEM(체인 없음) + 암호 없는
PKCS#8 키. 파일은 저장소 밖 `~/.config/today-peak/certs/`에 두고 시크릿에만 넣었다.
**만료 2027-09-30** — 만료 전에 `todaypeakprod02`를 먼저 발급해 올린 뒤 교체한다(무중단).

배포 상태: Supabase 프로젝트 `bzbjrjndfxoblalqjvhn`(Today Peak, ap-northeast-2)에 `login` 배포됨.
`VITE_API_BASE=https://bzbjrjndfxoblalqjvhn.supabase.co/functions/v1` (.env, 커밋 안 함).
시크릿 3개 등록 완료: `SESSION_SIGNING_SECRET`, `TOSS_MTLS_CERT`, `TOSS_MTLS_KEY`.
`supabase functions deploy`가 `src/lib/session-token.ts`를 같이 올렸다 —
**함수 디렉터리 밖 파일 import가 된다.** 세션 3b에서 `verify.ts`를 공유할 때 이 방식 그대로 쓴다.

토스 로그인 연결 끊기: 아래 "(결정, 뒤집힘)" 참고. 콜백을 등록했다.

콘솔 scope가 `USER_NAME` 필수로 잡혀 있다. 우리는 `userKey`만 쓰므로 이름은 받지 않는 게 맞다
(2026-01-02부터 `user_key`가 scope에 자동 포함). 검수 전에 조정 여부 확인.

SDK: `appLogin`은 deprecated다. `TossAuth.login()` / `TossAuth.isIntegrated()`를 쓴다.

**(2026-09-05) 실기기 검증 통과.** 번들 `20260905-11`을 올려 폰에서 확인했다.
토스 로그인 → 홈 진입 → 산 상세의 집계 줄이 보였다. 집계는 세션 토큰이 유효할 때만 응답하는
`mountain-stats` 호출이라, **로그인 전체 사슬(인가 코드 → mTLS 토큰 교환 → userKey → 서명 토큰
→ 서버 검증)이 실제로 돈다는 증거**다. `정상 인증하기`도 서버 판정("정상까지 10.9km")으로 끝났다.
남은 미검증은 실제 산 정상 인증과 `low_accuracy` 뿐이다.
Edge Function 로그는 CLI로 못 본다(`supabase functions`에 logs 서브커맨드 없음) — 대시보드에서만.

**(2026-09-05) 세션 무효화·재로그인·userKey 유지 확인.** `SESSION_SIGNING_SECRET`을 교체하니
폰에서 즉시 로그인 화면으로 돌아갔다(앱 시작 시 세션 확인 → 401 → `expireSession`). 다시 로그인하자
토스 앱의 "토스로 연결한 서비스"에 "오늘 정상(앱인토스)"이 재등록됐고, **연결을 끊기 전에 만든
스탬프가 그대로 보였다** — 연결 해제 후 재연결에도 토스가 같은 userKey를 준다는 뜻이다.
로그인 화면의 "휴대폰을 바꿔도 그대로 남아요" 문구가 사실로 확인됐다.

**(2026-09-05) 연결 끊기 콜백 실기기 검증 완료.** 토스 앱에서 연결을 끊으니 로그에
`unlink 요청 도착 · GET · ?userKey=…&referrer=UNLINK` → `unlink · userKey=… · referrer=UNLINK`가
찍혔고, 미니앱 재진입에서 로그인 화면이 다시 떴다. 콜백 → 행 삭제 → `unlinked_users` 기록 →
토큰 무효 → 401 → 로그인 화면까지 전부 동작한다.
재로그인 후 내 스탬프가 비어 있는 것까지 확인했다 — 콜백이 그 유저의 행을 실제로 지운다.
**이로써 비게임 출시 가이드의 토스 로그인 5개 항목이 전부 실기기 검증 완료다.**
콜백은 **GET + 쿼리스트링**으로 온다. Basic Auth는 콘솔에 넣은 값이 그대로 왔다(감싸지 않음).
첫 시도에서 실패한 이유는 함수가 401·400을 조용히 반환해 로그가 없었기 때문 —
지금은 어떤 요청이든 도착 즉시 한 줄을 남기고, 인증 실패 시 값 대신 SHA-256 지문만 남긴다.

**(결정, 뒤집힘) 연결 끊기 콜백을 만든다.** 처음에는 함수 5개를 지키려고 개인정보처리방침 문구로
처리하기로 했으나, 그 결정의 전제(`TossAuth.isIntegrated()`가 연결 해제를 감지한다)가 실기기에서
깨졌다. `toss-unlink` 함수 + `unlinked_users` 테이블 + 토큰 검증의 무효화 조회로 바꿨고,
개인정보처리방침도 "연결 해제 시 지체 없이 파기"로 되돌렸다(통지 유실 대비 요청 경로는 남김).
콘솔 `callback`은 `GET .../functions/v1/toss-unlink`로 등록했고 약관·스코프는 그대로 유지했다.

---

## 세션 3b — 테이블 + 나머지 Edge Function + 클라이언트 연결

```
CLAUDE.md 읽고 진행해줘.

1. 테이블 stamps, hike_starts (CLAUDE.md 컬럼 그대로). RLS 켜고 anon/authenticated 전부 차단.
2. Edge Function verify-summit: 세션 토큰 검증 → 클라이언트가 보낸 reading(좌표·accuracy·altitude·altitudeAccuracy)으로
   서버에서 verifySummit 재계산 → ok면 insert. 같은 산 같은 날 있으면 already_today. verify.ts는 클라이언트와 같은 파일을 import.
3. Edge Function my-stamps: 내 스탬프 목록. mountain-stats: 오늘 인증 수 / 누적 / 최근 6시간 등산 중 수.
   (start-hike는 세션 4에서)
4. 클라이언트 인증 흐름을 Edge Function 호출로 교체. 클라이언트 판정은 즉시 피드백으로만 쓰고 최종 결과는 서버 응답을 따른다.
5. 호출이 네트워크 오류로 실패하면 위치를 다시 읽지 않고 같은 reading으로 "다시 저장" 버튼. reading은 그 화면에 머무는 동안만 메모리에.
6. 산 상세 하단에 고정 문구: "정상에서 인터넷이 안 되면 신호가 잡히는 가까운 지점에서 시도해 주세요".
7. 세션 2의 디버그 표시는 제거해줘.
```

### 기록 (2026-09-05)

인증서 대기 중이라 실기기 로그인을 못 한다. 대신 **서명키로 만든 테스트 세션 토큰**으로
배포된 함수를 curl로 검증했다 (토큰 발급 도구는 저장소가 아니라 스크래치패드에 둔다).
확인된 것: 토큰 없음 401 / 위조 토큰 401 / 반경 밖 rejected / 30초 넘은 reading stale_reading /
정상 좌표 저장 / 같은 날 already_today / anon 키로 테이블 직접 접근은 42501 거부.

`verify_jwt = false`를 함수마다 켜야 한다. Supabase 게이트웨이가 Authorization 헤더를
자기 JWT로 검사해서, 우리 세션 토큰을 넣으면 함수에 닿기 전에 막힌다.

브라우저 확인은 Vite 프록시로 한다. 프로덕션 CORS 허용 목록(tossmini origin 4개)에
localhost를 넣지 않으려고 개발 서버에서만 같은 출처로 태운다 (vite.config.ts).

### 실기기에서 잡은 버그 (2026-09-05 저녁)

- **서버가 테스트 장소를 몰랐다.** 클라이언트는 `test-places.json`을 번들에 넣는데 Edge Function은 `mountains.json`만 import했다. 그래서 테스트 장소에서 인증하면 클라이언트는 "스탬프를 획득했어요"를 띄우고 곧바로 서버가 `400 invalid_request` → "저장하지 못했어요"로 뒤집혔다. 실제 산이 아닌 곳에서만 나는 증상이라 curl 검증(실제 산만 사용)에서 안 잡혔다.
  → `_shared/mountains.ts`가 `mountains.json` + 환경변수 `TEST_PLACES`를 합친다. 실측 기간에만 시크릿으로 넣고 **출시 전 `supabase secrets unset TEST_PLACES`** 로 지운다.
- **권한 다이얼로그 연타 오판.** 걸으면서 인증 버튼을 여러 번 누르니 권한이 허용돼 있는데도 "위치 권한이 필요해요"가 떴다. 다이얼로그가 이미 떠 있는 상태에서 또 호출되면 `denied`가 돌아온다.
  → 요청을 하나로 합치고(single flight), 다이얼로그 답이 `denied`여도 `getPermission()`을 다시 읽어 실제 상태로 확정한다.
- **(확정) `TossAuth.isIntegrated()`는 연결 해제를 반영하지 않는다.** 실기기 진단 결과
  `isIntegrated=true · isSupported=true · toss=5.272.0` — 지원 버전(5.237.0 이상)인데도, 토스 앱의
  "토스로 로그인한 서비스" 목록에 우리 앱이 **없는 상태에서** true를 돌려줬다. 클라이언트만으로는
  연결 해제를 감지할 수 없다는 뜻이고, `toss-unlink` 콜백이 유일한 경로다.
- **연결을 끊은 뒤 재로그인을 안 하면 서비스가 토스 목록에서 사라진다.** 우리 앱이 저장된 세션 토큰으로 계속 동작해서 `TossAuth.login()`을 다시 부를 일이 없었기 때문이다. 그래서 "토스로 로그인한 서비스"에 항목이 없어 연결 끊기 테스트를 다시 할 수 없었다. 테스트를 위해 `SESSION_SIGNING_SECRET`을 교체해 기존 세션을 전부 무효화하면 로그인 화면이 다시 뜬다.

**검증:** 폰에서 인증 → Supabase 대시보드에 행 생김, user_id가 userKey → 앱 삭제 후 재설치해도 스탬프 유지. 같은 날 재인증 차단.
curl로 세션 토큰 없이 verify-summit 호출 → 401. 반경 밖 좌표를 보내면 서버가 거부.
인증 성공 직후 비행기 모드 → 저장 실패 → "다시 저장" → 해제 → 성공.

---

## 세션 4 — 내 스탬프 + 문구 검토

```
CLAUDE.md 읽고 진행해줘.

1. 내 스탬프: 인증한 산 목록(이름, 날짜). 빈 상태 안내. 스탬프 그래픽은 공통 1종.
2. 산행 시작 체크인 (CLAUDE.md 참고): trailheads가 있는 산에만 "산행 시작" 버튼. 위치 1회 읽기 →
   가장 가까운 입구와 거리 ≤ START_RADIUS_M(500)이면 hike_starts insert, 밖이면 "등산로 입구에서 눌러주세요 ({입구명}까지 Nm)".
   nearestTrailhead(reading, mountain)는 순수 함수로 src/lib에, Node 테스트 포함. 하루 같은 산 1회. RLS는 stamps와 동일.
   정상 인증 흐름은 건드리지 마. 시작 안 눌러도 정상 인증이 그대로 되는지 테스트로 확인해줘.
3. 산 상세 집계 두 줄:
   - "지금 N명 등산 중": hike_starts에서 최근 6시간. N = 0이면 줄 숨김.
   - "오늘 N명이 정상을 찍었어요": stamps에서 오늘. N = 0이면 대신 누적 "지금까지 M명 인증".
   0명이라는 문구는 어떤 화면에도 나오지 않게 해줘. mountain-stats 응답을 그대로 표시.
4. 홈 ↔ 산 상세 ↔ 내 스탬프 네비게이션. 앱인토스 네비게이션 바 가이드에 맞춰줘.
5. 화면 문구 전체를 CLAUDE.md 정책 항목에 비춰 검토하고 걸리는 것 목록으로 알려줘.
```

**검증:** 세 화면 왕복. 샘플 입구 좌표를 내 위치로 교체 → 시작 → "지금 1명 등산 중". 1km 떨어진 좌표에선 거부.
trailheads가 빈 산엔 버튼 없음. 시작 없이 정상 인증 성공.
본인 인증 후 "오늘 1명". 인증 없는 산엔 누적 문구만. 문구 위반 없음. mountains.json에 실제 데이터 채워짐(사용자 작업).

---

## 세션 4.5 — 디자인 패스 + 등록용 이미지 자산

화면이 다 나온 뒤에 한 번에 다듬는다. 세션 2~4에서는 꾸미지 않는다 (다시 손대게 되므로).

```
/ait:design

세 화면(홈·산 상세·내 스탬프)을 디자인 가이드 기준으로 다듬어줘.
그리고 콘솔 등록용 이미지 자산을 만들어줘 — miniapp_create 스펙에 맞춰서
로고(600×600 각진 정사각형, 배경 필수), 썸네일, 스크린샷.
세션 2의 디버그 표시와 테스트 장소 항목이 남아 있으면 지우기 전에 알려줘.
```

### 기록 (2026-09-05)

스크린샷은 headless Chrome + CDP로 **실제 화면을 636×1048로 직접 캡처**했다
(ImageMagick·Pillow가 없어서 리사이즈 대신 정확한 뷰포트로 찍었다).
스크래치패드의 `shot.mjs`가 그 도구다. 캡처 동안 `test-places.json`을 잠시 치워
등록용 이미지에 실측 테스트 장소가 안 들어가게 했다.

**앱정보 검토 승인됨 (2026-09-05).** 세로 스크린샷 5장(인트로 카드 + 홈·상세·스탬프·로그인),
아이콘·다크 아이콘·키워드 10개·카테고리(생활>건강>건강 관리)로 `reviewState=APPROVED`.
가로 썸네일은 `images`에 안 들어갔지만 비게임은 필수가 아니라 통과했다.
→ 이제 mTLS 인증서를 신청할 수 있다.

정상 인증 **성공** 화면은 못 찍었다 — 테스트 사용자가 오늘 네 산을 이미 다 찍어서
같은 날 재인증이 막힌다(의도된 동작). 인증서를 받은 뒤 실기기에서 찍어 교체하면 된다.

**검증:** 하드 규칙 위반 없음 — 텍스트 11px 이하 없음, 터치 타깃 44px, 하단 CTA safe area 34px,
꺾쇠는 SVG, 상단 네비를 직접 그리지 않음. 콘솔에 올릴 이미지 3종이 규격대로 생성됨.

이 세션이 없으면 세션 5 검수 직전에 등록용 이미지가 없어서 막힌다.

### 등산로 입구 데이터 (2026-09-05 밤)

`trailheads`는 한국등산트레킹지원센터 **숲길POI**(공공데이터 15097947, `fmmtnFrtrlPoiInfoService`,
`srchPlaceTpeCd=ENTRY`)로 채웠다. 봉우리POI와 같은 `frtrlId`를 써서 이름 매칭 없이 붙는다.
전국 688건 → 좌표 완전중복 5건 제외 682곳, **96곳 중 87곳에 입구가 있다.**
입구 없는 9곳(내장산·대둔산·도락산·두타산·속리산·운장산·적상산·천성산·황장산)은 규칙대로 산행 시작 버튼이 안 뜬다.

- 공공데이터포털은 **API마다 활용신청이 따로**다. 키는 하나. 신청 안 한 API는 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`(코드 30)를 준다.
- 입구 이름이 "들머리"·"입구"·"날머리"처럼 밋밋한 게 90건쯤 된다. 문구에 그대로 쓴다("들머리까지 300m"). 이름을 지어 붙이지 않는다.
- 북한산 51·관악산 41·도봉산 37개처럼 입구가 많은 산은 "가장 가까운 입구" 규칙이 그대로 흡수한다. 스키마 변경 없음.
- 번들은 js 288→329KB(gzip 88→101KB). Edge Function `start-hike`·`verify-summit`도 같은 JSON을 묶으므로 재배포했다.
  서울시청에서 북한산 start-hike → `too_far · 구기터널.삼성출판박물관 · 5064m`로 서버 확인.
- 같은 API의 PARK·TOILET·SHELTER·SPRING·STORE·INFO 타입이 v1 시설 데이터다. 코스 선형은 API가 아니라 GPX 파일(15098177, 15108086).

---

## 세션 5 — 검수

```
비게임 출시 가이드 체크리스트를 문서에서 가져와 항목별 충족 여부를 표로 정리해줘.
미충족 항목은 고치기 전에 먼저 알려줘. 모두 충족하면 검수를 요청해줘.
```

**검증:** 승인 메일 → 콘솔 출시하기.

**콘솔 제약 (2026-09-05 확인):**
- 사용 연령 19~99세는 우리 앱 설정이 아니라 **플랫폼 제약**이다 — 공식 문서: "지금 앱인토스는 만 19세 이상 사용자에게만 제공돼요". 그래서 콘솔에서도 못 바꾸고 "추후 제공"으로 안내된다.
- 토스 로그인 설정의 개인정보처리방침 항목에 `agreementUnder14UsageStatus: "USED"`가 기본으로 붙어 있다. 이 필드의 정의는 공식 문서에 없다. **앱인토스가 연령 제한을 낮추는 시점에 다시 볼 것** — 만 14세 미만의 개인정보를 받으려면 법정대리인 동의 절차가 필요한데 우리 앱에는 없다.
- 워크스페이스 약관·토스 로그인 약관은 대표관리자만 동의할 수 있고, 대표관리자 확정은 사업자 검토 완료가 선행되어야 한다. 세션 3a의 실제 병목.
- `miniapp_update_basic_info`는 저장이 아니라 **검토 요청**이다. 아이콘(600x600 PNG)과 검색 키워드가 필수라 그것들이 준비된 뒤에 한 번만 호출한다.

---

### 디자인 적용 + 번들 17 실기기 확인 (2026-09-06)

`docs/design-brief.md` 의뢰서 → 앱빌더 납품(`design/`) → 코드 적용(커밋 130d393). 번들 `20260906-17`.
실기기에서 전부 정상: 성공 티켓 애니메이션, 제안 입력 화면의 키보드 대응, 산행 시작 버튼(실제 산에 뜸),
플로팅 탭바가 컬렉션 마지막 줄을 가리지 않음, 로그인 인트로.

- 로그인 인트로는 세션 토큰(14일)이 살아 있어 처음엔 못 봤다. 시크릿을 한 번 더 교체해서 확인했다.
  세션 시크릿 교체는 이번이 세 번째 — 실기기에서 로그인 화면을 다시 보는 가장 빠른 방법이다.
- 탭바는 납품 디자인(하단 부착형) 대신 플로팅 알약. 앱인토스 UI/UX 가이드 "토스가 제공하는 플로팅 형태" 규칙.
- 스크린샷 검증은 헤드리스 Chrome에 `localStorage['__ait_storage:today-peak.session']`으로 토큰을 넣고
  `window.__ait._state.location.coords`를 바꿔 실제 인증 흐름을 돌리는 방식. 스크래치패드 `shot.mjs`.

### 출시 전 정리 (2026-09-06)

- `supabase secrets unset TEST_PLACES` 완료. `src/data/test-places.json`은 저장소 밖(스크래치패드)으로 옮겼다.
  빌드 결과에 `[테스트]`·`test-home` 문자열이 없는 것을 확인. 목록 96곳, 실측 각주 자동 소멸.
- DB 테스트 행: stamps 9(+검증 캡처 중 생긴 북한산 1), hike_starts 1, peak_suggestions 3. **삭제 SQL은 자동 모드
  분류기가 막아서 사용자가 직접 실행한다.** `unlinked_users` 1행은 정상 상태라 남긴다.
- 등록용 스크린샷 6장을 새 디자인으로 다시 만들었다(`assets/screenshot-1~6`). 375×618 뷰포트를 dsf 1.696으로
  찍어 636×1048 정확히. 인트로 카드는 `reg/intro.html`(종이색 배경 + 성공 티켓). 콘솔 교체는 앱정보 재검토를
  부르므로 사용자 결정.
- 릴리즈 판정: `reviewRequestDecision=ALLOWED`, `releaseDecision=WORKSPACE_TERMS_AGREEMENT_REQUIRED_FROM_OWNER`
  → 번들 검수 요청은 가능하지만 **출시하기 전에 대표관리자가 콘솔 웹에서 제휴 약관에 동의**해야 한다.

### 검수 직전 (2026-09-06)

- DB 테스트 행은 사용자가 SQL Editor에서 직접 지웠다(stamps·hike_starts·peak_suggestions 0건 확인).
- 출시 후보 번들 `20260906-18`(테스트 장소 없음) 업로드·테스트 푸시 완료.
- 스크린샷 6장을 `miniapp_update_screenshots`로 교체 → 앱정보 `IN_REVIEW`. `miniapp_get`은 승인 전까지
  옛 이미지를 돌려준다(검토 중인 페이로드는 조회에 안 나온다).
- 번들 검수 요청은 사용자가 콘솔 웹에서 직접 한다. 릴리즈 노트 초안은 아래.

### 산별 스탬프 v0.1 (2026-09-06, 출시 후 첫 업데이트용)

`docs/design-brief-stamps.md` + 2차 `design-brief-stamps-seasons.md` → 납품 `design/stamps/`(10곳 × 4계절 + 모티프 9종).
- `scripts/build-stamp-art.mjs`가 `src/data/stamp-art.json`(96KB)을 만든다. 모티프 `<use href="x.svg#…">`를 문서 안 `<symbol>`로 인라인.
- `src/lib/stamp-art.ts`(순수, 테스트 7개): id 접두사, 40px에서 `lg-only` 제거, 미모음은 `collected-only` 제거 + 전부 `#D4DACE` 단색 + 불투명도 제거.
  중첩 `<g>`를 지우려고 태그를 세는 함수를 썼다. 정규식 하나로는 못 한다.
- `Stamp.tsx`: 그림이 있는 산은 `dangerouslySetInnerHTML`, 없는 86곳은 절차적. **절차적도 같은 계절 팔레트**(붉은 단풍형)로
  능선 그라데이션·눈 규칙(겨울만, 봄은 1500m↑ 잔설)을 따르게 해서 두 체계가 형제로 보인다.
- 검증은 react-dom/server로 시트를 그려 헤드리스 캡처. **셀마다 따로 렌더하면 `useId`가 겹쳐 clipPath가 섞인다** — 한 트리로 렌더해야 한다.
- 번들 js 333→439KB(gzip 102→111KB). 미모음 시트에서 그림이 흐린 건 의도(빈 자리).
- 나머지 86곳 디자이너 확장은 `design/stamps/docs/EXPANSION.md` 규칙대로. 팔레트 유형(red/gold)만 배정하면 된다.

### 계절 컬렉션 + 검색 (2026-09-06, v0.1)

- 칸은 산당 하나 그대로. 스탬프는 **가장 최근 계절**로 그리고 이름 아래 **점 4개**(봄·여름·가을·겨울)로 모은 계절을 표시.
  칸을 누르면 `SeasonSheet`(바텀시트)에 사계절 4장 + 날짜. 같은 계절 재인증은 **첫 인증 날짜**를 유지(`groupSeasons`).
- 진행도 "N곳 중 M곳 · 사계절 완성 K곳". 산 상세 배지가 "봄·가을 스탬프를 모았어요", 성공 티켓에 "가을 스탬프 · 사계절 중 2".
- 서버·DB 변경 없음. `stamps`가 이미 산당 여러 행(하루 1회)이라 클라이언트에서 접기만 했다.
- 홈 검색: 칩 위 입력칸. 공백 무시, 이름·인증 지점 부분 일치. 권역 칩과 AND.
- 시트는 `useSystemBack(true, onClose)`로 뒤로가기에 닫힌다. 탭 화면에서 유일한 구독자라 충돌 없음.

번들 `20260906-19`(v0.1: 산별 스탬프·계절 컬렉션·검색) 실기기 확인 완료(2026-09-06). 원격 푸시됨.
18 승인 뒤 19를 다음 검수에 넣는다.

### 동네 명산 컬렉션 (2026-09-06, v0.2)

- `scripts/build-local-mountains.mjs` → `src/data/mountains-local.json` 수도권 30곳. OSM Overpass를 다섯 상자로 나눠 받는다(한 번에 받으면 504).
  `--cache` 덤프로 재현 가능. 동명이산은 `near` 힌트에 가장 가까운 노드, 3km 넘게 떨어지면 문제로 보고.
- `Mountain.collection`('top100' 기본 | 'local'). 클라이언트·서버 `MOUNTAINS`에 둘 다 합쳐 인증 로직은 그대로.
- UI: 내 스탬프 상단 세그먼트 `100대 명산 / 동네 명산`(진행도·권역 따로). **홈은 컬렉션을 구분하지 않는다** —
  칩은 시·도(서울·경기·인천) + 권역(강원·충청·전라·경상·제주). `province` 필드는 수도권 산에만 있고 `areaOf`가 칩 단위를 정한다.
  성공 티켓 "N곳 중 M곳"도 그 산의 컬렉션 기준.
- 입구 데이터 없음 → 산행 시작 버튼 없음. 확장 규칙대로 절차적 스탬프.
- 서버 재배포(verify-summit·start-hike). 서울시청→인왕산 `too_far 2713m` 확인.
- 남은 것: 앱 설명·키워드에 동네 명산 반영(앱정보 재검토), 개인정보처리방침/설명에 OSM 출처 한 줄.

### 산 정보 확장 준비 (2026-09-06)

- **TourAPI 관광사진** `https://apis.data.go.kr/B551011/PhotoGalleryService1/gallerySearchList1`(keyword, MobileOS=ETC, MobileApp, _type=json).
  126곳 검색: **81곳 있음 / 45곳 없음**(100대 26 + 동네 19). 없는 목록은 사용자가 직접 구한다. 공공누리 1유형, 촬영자 표기.
- **토스쇼핑 쉐어링크 오픈 API 있음** (`https://sharelink.toss.im/openapi`, 문서 sharelink-docs.toss.im/guide/open-api).
  승인 사업자만 발급(크리에이터 어드민 > API 연동, 영업일 5일, 서비스명·URL·용도·화면 시안 제출). Access/Secret Key, 서버 IP 등록(최대 10개) 필수 →
  Edge Function 고정 IP가 없어서 **IP 등록이 걸림돌**. 엔드포인트: `POST /openapi/links`(tacaItemId→shortUrl), `GET /openapi/products/detail`(이름·가격·이미지·재고),
  카테고리 베스트·오늘의 특가·베스트셀러 목록. **키워드 검색 없음**. 정책: 가격을 DB에 쌓아 이커머스처럼 나열 금지, 이미지 저장·외부 앱 표시는 사전 승인,
  고지 문구 "토스쇼핑 쉐어링크 활동으로, 링크 구매 시 수수료를 지급받습니다"를 상품 소개 가까이에.
- 네이버 지역 검색은 **네이버 클라우드 플랫폼(API Hub)**으로 옮겨감. `GET https://naverapihub.apigw.ntruss.com/search/v1/local?query=&display=5&sort=comment`,
  헤더 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY`. 요청당 최대 5건, 하루 25,000회, 반경 없음(검색어), 좌표는 WGS84(문서 표기).
  Edge Function에서 호출(키 비공개). `NAVER_CLIENT_ID/SECRET` secrets.
- 쉐어링크 신청용 시안 `assets/sharelink-mockup.png`(산 상세 + '챙길 것' 섹션, 상품은 예시).

### 오늘 Pick 탭 (계획, 쉐어링크 승인 대기)

탭 3개로 간다: 홈 / 내 스탬프 / **오늘 Pick**. 산 상세에 쇼핑까지 얹으면 사진·시설·코스·맛집이 들어올 자리가 없다.
- 오늘 Pick: 카테고리 칩(전체·장비·의류·음식·안전) + 카테고리별 가로 카드. 운영자가 고른 20~30개.
  **카테고리 편집 형태**라 "가격을 쌓아 이커머스처럼 나열"(쉐어링크 반려 사유)과 구분된다.
- 산 상세: "추천 물건" 섹션에 카드 3개 + '더 보기' → 오늘 Pick 탭. 계절·고도로 다른 3개를 고른다.
- 고지 "토스쇼핑 쉐어링크 활동으로, 링크 구매 시 수수료를 지급받습니다"를 두 화면 모두 카드 위에.
- 시안: `assets/sharelink-mockup.png`(상세), `assets/sharelink-mockup-pick.png`(탭). 신청서 참고 이미지로 제출.
- 네이버 검색 키 확인됨(ncloud). 레거시 `openapi.naver.com`은 401 — API Hub만 동작.
  좌표는 카텍 정수(1270079750 → 127.0079750), 1e7로 나눠 WGS84.

### 내려와서 — 산 근처 음식점 (2026-09-06)

- `mountain-stats`가 네이버 API Hub 지역 검색을 함께 부른다(함수 6개 상한 유지). 실패하면 `places: []` — 집계와 같은 원칙.
- `src/lib/places.ts`(순수, 테스트 5개): 강조 태그 제거, 카테고리 한 칸, 주소는 구·도로명까지, 이름 중복 제거.
  **검색어는 2단계** — 들머리 이름에서 "입구·사전예약·탐방지원센터" 등을 떼고 앞 두 단어로 먼저, 결과가 0이면 산 이름으로.
  ("북한산 우이령길 사전예약 입구 맛집"은 0건이었다.)
- 저장하지 않는다. 화면을 열 때마다 조회하고 이름·업종·주소만 보여준다. 외부 링크 없음(앱인토스 정책).
- 카드 안에서 **음식점 / 카페**로 나눈다. 네이버는 요청당 5건이라 "맛집"·"식당"·"카페" 세 번 부르고
  카테고리(`카페|디저트|베이커리|빵|커피`)로 갈라 각 5곳까지. 한쪽이 비면 토글을 숨기고 있는 쪽만 보여준다.
- 확인: 북한산(양주 장흥면), 설악산(속초 순두부·카페), 인왕산(종로 서촌·부암동) 모두 그 동네 가게가 나온다.
  도심 산은 카페가 많아 음식점이 1~2곳뿐일 수 있는데, 검색 결과가 그런 것이라 그대로 둔다.

### 상세 화면 정리 (2026-09-06, 이용자 피드백)

- **접이식 카드** `FoldCard`. 내려와서를 접어 두고 제목 옆에 "8곳"만 보여준다. 사진·시설·코스·추천 물건이 붙어도
  인증 버튼이 아래로 밀리지 않게 하는 게 목적. 펼쳤을 때만 카드 안 조작(음식점·카페 토글)이 보인다.
- **산행 시작 → "나도 등산 중이라고 알리기"**. 하단 CTA에서 빼고 집계 카드 안으로 옮겼다.
  지인 피드백: 두 버튼 차이가 애매하고 "산행 시작을 해야 인증이 되는지" 헷갈린다.
  버튼 아래에 "등산로 입구에서 누르면 위 집계에 함께 잡혀요. 정상 인증과는 상관없어요."를 상시 표시.
  집계 줄이 0이면 구분선을 그리지 않는다.

### 오늘 Pick 껍데기 (2026-09-06, 쉐어링크 심사 중)

승인 전까지 아무것도 노출되지 않게 **데이터로 켜고 끈다**. `src/data/picks.json`의 `items`가 비면
탭도 카드도 렌더되지 않는다(빈 탭은 막다른 화면이라 탭바에서 아예 뺀다). 승인 후 어드민에서 발급한
쉐어링크를 이 파일에 채우면 켜진다 — 코드 수정 없음.

- `src/lib/picks.ts`(순수, 테스트 3개) + `picks-data.ts`(JSON만 읽음. Node 테스트가 JSON import 속성을 요구해서 분리).
- `Picks.tsx`: 카테고리 칩(전체·장비·의류·음식·안전) + 2열 그리드. 고지 문구는 칩 위 상시.
- 산 상세: 접이식 "추천 물건" 카드 = 계절·고도로 고른 3개 + '더 보기' → 탭. `picksForMountain`이 순서를 정한다.
- 링크는 `openURL(pick.link)`로 이용자가 카드를 눌렀을 때만 연다(자동 실행 금지).
- 스킴에 `?tab=picks` 추가.
- 확인: 샘플 5개를 넣으면 탭·카드가 나오고, 비우면 탭바가 `홈|내 스탬프`로 돌아간다.

## 백로그 — 데이터 검증 (출시 전, 사람이 확인)

`node scripts/build-mountains.mjs`가 매번 같은 리포트를 낸다. 고치는 방법은 스크립트의 정상 선택 규칙에
예외를 두거나(산 id → 봉우리 id 고정 표), 다른 출처의 좌표를 가져오는 것이다. 좌표는 지어내지 않는다.

- **확인 필요 7곳** — 공식 높이와 채택 봉우리 고도 차 60m 초과:
  금산(채택 "남해 금산 정상" 0m)·지리산(통영)("가마봉 정상" 0m)은 고도가 없는 봉우리라 위치 자체는 맞을 수 있다.
  무등산(서석대 1119m — 천왕봉이 군사시설이라 실제 인증 지점은 서석대가 맞을 듯)·
  무학산(중봉 482m)·변산(옥녀봉 443m)·장안산(1174m)·천성산(산정상 550m)은 봉우리POI에 진짜 정상이 없거나 다른 이름일 수 있다.
- **좌표 없는 4곳** — 남산·대암산·선운산·점봉산은 봉우리POI에 없어 96곳에 빠져 있다.
  컬렉션을 96으로 갈지, 다른 출처(국토지리정보원 등)에서 좌표를 구할지 결정.
- **덕유산 인증 지점이 중봉(1594m)** — 진짜 정상은 향적봉(1614m)이다. 봉우리POI의 향적봉 고도가 공식 높이와
  60m 넘게 어긋나 "가장 가까운 고도" 규칙이 중봉을 집었을 가능성. 산 id → 봉우리 고정 표로 덮어써야 한다.
- **입구 없는 9곳** — 내장산·대둔산·도락산·두타산·속리산·운장산·적상산·천성산·황장산. 숲길POI ENTRY에 없다.
  산행 시작 버튼이 안 뜰 뿐 인증엔 영향 없다. 입구노면정보(15097961)에 있는지 확인.
- **난이도 29곳만** — 숲나들e 난이도 필드가 대부분 비어 있다. 필터에서 "미표기"로 두거나 다른 출처 필요.
- 이용자 **정상 제안**(`peak_suggestions`)이 쌓이면 위 항목의 근거로 쓴다.

---

## v1 계획 (2026-09-06, 번들 19 검수 요청 후)

번들 `20260906-19` 검수 승인, **2026-09-06 출시**.

### 작은 산 데이터 출처 비교 (2026-09-06 실측)

| 출처 | 결과 | 판단 |
|---|---|---|
| **OpenStreetMap** Overpass `natural=peak` | 수도권 박스(37.40–37.72, 126.80–127.20)에서 이름 있는 봉우리 295개. 인왕산 339m·안산 296m·아차산·청계산 618m·남산·불암산·수락산·우면산·대모산·검단산 좌표+고도 있음. **동명이산 많음**(안산 3개, 천마산은 141m짜리 다른 산), 광교산·소요산·계양산·수리산은 박스 밖이거나 이름 표기 다름 | **후보 추출용 1순위.** 손 검수 필수. ODbL — `mountains-local.json`에 출처 표기 |
| 한국등산트레킹지원센터 전국 주요 봉우리 POI (15108062~69) | 100대 명산 밖도 포함한다고 명시. 엔드포인트는 활용신청 후 페이지에서 확인해야 함(스크립트 렌더라 긁히지 않음) | **활용신청 해볼 가치 있음.** 되면 지금 파이프라인 그대로 |
| 국토교통부 국가지명 API (15058628) | 국가기본도 지명 좌표. 활용신청 필요 | OSM 후보의 **검증**용. 지명 위치가 정상석과 다를 수 있음 |
| 산림청 산정보 서비스 (15058662) | 3,368개 산 이름·높이·설명. 좌표 없음(확인 필요) | 설명 텍스트 보강용. 좌표 출처는 아님 |

(2026-09-06 확인) 전국 주요 봉우리 POI는 `https://apis.data.go.kr/B553662/culturalInfoService/getCulturalInfoList`.
3,854건이지만 전부 **문화자원 지점**(암자·사찰)이고 258개 산은 100대 명산 계열. 인왕산·안산·청계산·불암산 없음 → 작은 산 출처로 부적합.
국가지명은 쓰지 않기로 함.

결정: **OSM만** 쓴다. 후보를 뽑고 유명한 곳부터 30곳 손으로 확정. 좌표를 지어내지 않는 규칙은 그대로.

### 두 번째 컬렉션 이름

100대 명산과 나란히 놓이는 판. 후보와 판단:
- **동네 명산** — "명산"을 공유해 한 앱으로 읽히고, "동네"가 가까움·가벼움을 준다. **추천**
- 근교 명산 — 정확하지만 딱딱하고 지방 이용자에겐 "근교"가 서울 기준으로 들린다
- 동네 산 — 가장 친근하나 100대 명산 옆에서 격이 낮아 보인다
- 가까운 산 — 홈의 "가까운 산부터"와 겹쳐 헷갈린다

### 순서

| # | 할 일 | 크기 | 비고 |
|---|---|---|---|
| 1 | **출시 직후 확인** — 실제 정상 인증 첫 사례, 대시보드 DAU·에러, 제안 창구에 쌓이는 좌표 | 소 | 한 주 |
| 2 | **데이터 고정표** — 무학산·천성산·덕유산(향적봉) 정상, 좌표 없는 4곳, 입구 없는 9곳 | 소 | 산 id→봉우리 id 표 하나. 제안 창구 데이터로 근거 |
| 3 | **스탬프 86곳 확장** — 디자이너에게 `design/stamps/docs/EXPANSION.md`대로 권역 단위 의뢰(수도권 12 → 강원 → …) | 중 | 팔레트 유형만 배정. 코드 변경 없음 |
| 4 | **동네 명산 컬렉션** — (a) OSM+국가지명 파이프라인 `scripts/build-local-mountains.mjs` (b) 컬렉션 분리 UI: 내 스탬프 상단 세그먼트 `100대 명산 / 동네 명산`, 홈 칩에 "동네" (c) 서버는 `mountains.json`에 `collection` 필드만 추가 (d) 앱 설명·키워드 갱신 → 앱정보 재검토 | 대 | 수도권 30곳부터. 정체성 흐려지지 않게 100대 명산 진행도는 그대로 |
| 5 | **성공 후 리뷰 요청** `requestReview` — 만족 시점에 1회 | 소 | 문서 규칙: 노출 보장 없음, 흐름에 의존 금지 |
| 6 | **공유** — 내 스탬프 캡처/`intoss://today-peak` 링크. 유일한 성장 경로 | 중 | `intoss-private://` 금지 |
| 7 | **산 정보 확장** — 숲길POI PARK·TOILET·SHELTER·SPRING(이미 승인된 API), GPX 코스 | 중 | Supabase 테이블. 데이터 3층 규칙 |
| 8 | **토스 포인트 프로모션** — 시작 5P·정상 10P 서버 지급 | 중 | 1인 5,000P 상한, 랜덤 금지. 콘솔 프로모션 검토 필요 |

4는 3과 병렬 가능(디자이너 기다리는 동안). 5·6은 반나절짜리라 2 다음에 끼워 넣는다.

---

## v1 후보 (출시 후, 데이터 보고 결정)

- **산 정보 확장** (2026-09-05 추가, 벤치마크 미니앱 참고): 코스 목록(거리·소요시간·누적 오르막·칼로리·난이도),
  코스 상세, 경사 프로파일(지도 없이 SVG로 가능), 시설·볼거리, 준비물·유의사항, 즐겨찾기.
  데이터는 Supabase 테이블에 두고 산 상세에서 조회한다(CLAUDE.md "데이터 구조 3층").
  수집 난이도가 갈린다 — 산 이름·지역·고도는 쉽고, 정상 좌표와 등산로 선형은 중간,
  화장실·약수터·사찰 같은 시설은 어렵다. 소요시간·칼로리는 거리+누적상승에서 계산한다.
  지도 경로 렌더링은 계속 범위 밖.

- **산별 스탬프 그래픽**: 인증 수 많은 산부터. 개발이 아니라 에셋 작업.
- **보상형 광고**: 인증 성공 화면. `userEarnedReward`에서만 지급. 버튼 문구 3안 제안받고 선택.
- **완주 기록**: 세션 4의 hike_starts와 stamps를 연결해 시작→정상 경과 시간을 스탬프에 표시. 필수 아닌 보너스.
- **유입용 프로모션**: "첫 산행 시작 하기" 1인 1회 100P 고정. 목적은 토스 혜택 탭 노출로 신규 유입. 검수 2~3일, 비즈월렛 충전, 사전 고지 문구.
- **산행 포인트**: 산행 시작 5P 즉시 + 정상 인증 10P. 합계 15P/산행. 지급된 포인트는 회수 불가이므로 "줬다가 줄이기" 구조는 쓰지 않는다.
  프로모션 두 개 별도 등록("산행 시작 하기", "정상 인증 하기"). 1인 하루 한도 필수.
  지급은 클라이언트가 아니라 Supabase Edge Function에서: hike_starts/stamps insert 성공 후 서버가 프로모션 API 호출 (문서 권장 S2S).
  정상 인증 프로모션이 "노동 요구"로 반려되면 시작 5P만 남기고 정상 보상은 스탬프로.
  누적 5,000P 상한 = 약 330회 산행, 실질 무제한. 예산 소진 시 자동 종료되므로 안내 문구 필수.
- **맛집 추천**: 데이터 소싱 먼저.
- **소셜 집계 확장**: 홈에 "지금 등산 중 많은 산 TOP 3", 최근 인증 아바타. (기본 집계는 세션 4에 포함)
  실시간 개인 위치 표시는 하지 않는다 — "근처 사용자 탐색"은 만남·소개팅 분류 및 위치정보사업 신고 리스크. 유저 간 연락 수단은 절대 붙이지 않는다.
- **스마트 발송 (재방문 푸시)**: 세그먼트 기반(예: 북한산 인증 유저, 30일 미방문). 위치 기반 근접 알림은 불가.
  광고성 재방문 푸시는 최근 30일 사용자 100명 이상부터. 건당 9.9원, 문구 검수 필요. 파트너 서버 API 발송 가능.
  용도는 스탬프 컬렉션의 빈 칸을 찌르는 문구("아직 안 찍은 서울 근교 명산 3곳")로 한정. 포인트와 결합하지 않는다.

### v1 — 피드백 반영·공유 카드·배치 1·완주 기록 (2026-09-06 밤)

- 폰 피드백 6건 반영: 홈 '안 모은 산 먼저' 정렬, 집계 인증 전 노출(0이면 문구), 같은 날 두 번째 인증의
  성공 화면 플래시(하루 한 번 규칙은 서버만 알아서 생김 → `stampedToday`로 낙관 표시 차단), 제보 후 돌아가기,
  내 스탬프 아래 '앱 평가하기', 내 스탬프 판에서 모은 산을 권역 안 앞으로.
- 공유 미리보기: `scripts/build-og.mjs`가 앱과 같은 `<Stamp>`로 산×계절 504장을 구워 `docs/og/`(GitHub Pages).
  800×420 PNG, 21MB. 파일 이름이 URL이라 매핑 없음. 헤드리스 크롬이 가끔 매달려 30초 제한+재시도.
  **파일 이름이 같아서 이미 공유된 링크는 옛 이미지가 캐시될 수 있다** — 그림이 크게 바뀌면 `og/v2/`.
- 동네 명산은 들머리가 없어 산행 시작 버튼이 안 떴다. 이 컬렉션만 정상 반경 800m(`LOCAL_START_RADIUS_M`).
  100대 명산 9곳(들머리 없음)은 그대로. `start-hike`를 안 올려서 폰에서 안 됐던 것도 잡음.
- 스탬프 배치 1(수도권 42) 통합. stamp-art.json 96→411KB, 번들 gzip 116→132KB. 배치 3까지 가면
  1MB 안팎이라 그때 산별 지연 로딩 판단. 2차 의뢰서 `docs/design-brief-stamps-batch-02.md`(계절 파일 2.5KB 상한).
- 완주 기록: 인증 전 24시간 안 같은 산 산행 시작과 짝(`src/lib/visits.ts`, 한국 날짜로 안 자름 — 야간 산행).
  티켓 "시작부터 N분 만에 정상", 사계절 시트 "N번 다녀왔어요" 목록, 상세 배지 "· N번".
- 대시보드 9/6 DAU 9. 문의·별점 0. peak_suggestions는 본인 테스트 2건뿐(지움). 실사용 인증 아직 0.
- 콘솔 앱정보: 스크린샷 6장(새 스탬프 그림)·부제·상세 설명·키워드 교체 → `IN_REVIEW`.
  성공 티켓은 375×618에서 절취선이 잘려 424×699 뷰포트(clip scale 1.5)로 찍음. 확인용 테스트 행은 지움.
- **`apis.data.go.kr` 장애**: 저녁 내내 TCP 타임아웃(www.data.go.kr은 200). 사진(TourAPI)·주차장(숲길POI)은
  대기. `scripts/build-photos.mjs --probe`로 필드 확인 후 진행. 번들은 이 둘까지 끝내고 올리기로.

### 사진·시설 (2026-09-07 오전, apis.data.go.kr 복구 후)

- `apis.data.go.kr`은 9/6 저녁~9/7 오전 TCP는 받되 TLS 응답이 없는 상태였다(폰 셀룰러에서도 동일 → 포털 장애).
- 스탬프 배치 2·3 통합 → 126곳 전부 고유 그림. 번들 gzip 152KB. 육안 검수는 `sheets/batch-03/09-all-126.png`.
- 들머리 없는 100대 명산 9곳 중 8곳은 숲길POI PARK(주차장)로 채움. 천성산만 남음. 입구 682 → 706.
- 사진: TourAPI 관광사진(공공누리 1유형, `이용허락범위 제한 없음`). 응답에 사진별 유형 필드는 없다.
  제목순 첫 결과를 쓰면 63장 중 20장이 전시관·표지판·사찰이라, 제목 필터 + 풍경 키워드 우선 + 육안 검수로
  15곳 SKIP + 원본 깨진 2곳 → **39곳**. 나머지 87곳은 사용자가 준다(`docs/photos-needed.md`,
  `src/data/photos-manual.json`으로 받음). 파일은 800px q70으로 줄여 `docs/photos/`(5MB), GitHub Pages 서빙.
  산 상세 hero = 사진 + 스탬프 + "사진 한국관광공사 · 촬영자".
- 시설: `facilities` 테이블(2,473행: 주차장 775·화장실 1,179·대피소 191·약수터 328, 94/96곳).
  `scripts/build-facilities.mjs --write` → `supabase/seed/facilities.sql` → `supabase db query -f`.
  **`db query`에 SQL을 인자로 주면 `--`로 시작하는 주석을 플래그로 읽어 help가 뜬다. `-f 파일`로 줄 것.**
  mountain-stats가 종류별 이름을 같이 준다. 관악산은 141곳(화장실 86)이라 이름 3개 + "외 N곳"으로 요약.
- 완주 기록·콘솔 앱정보 교체(`IN_REVIEW`)·대시보드 확인은 9/6 밤 항목 참고.

### 코스 (2026-09-07 저녁)

- 등산로 데이터는 한국등산트레킹지원센터 GPX 두 벌(15098177 100대명산 607파일 / 15108086 봉우리 코스 4,492폴더).
  포털 로그인 뒤에만 받아져서 사용자가 내려받았다. **트랭글 GPS 기록이지 편집된 코스가 아니다** — 북한산 36개
  중 백운대에 닿는 건 2개, 시작점 이름이 "갈림길"인 것도 있다. 벤치마크 앱("100대 명산 등산하기")도 같은 파일을
  그대로 썼다(1코스 북한산성유료주차장 9.21km가 파일 0001).
- `scripts/build-courses.mjs` → `courses` 테이블 635행(100대 96곳 + 동네 22곳), 지점 14,691. 트랙 좌표는 안 둔다.
  이름 = 시작 300m 안 입구·주차장·정류장, 없으면 "N.Nkm 코스". 정상 = PEAK 지점 최고점. 오르막 문턱 8m(벤치마크와
  일치: 982 vs 981m). 시간 = 4.5km/h + 오르막 10m/분. 칼로리 70kg. 난이도 오르막 300/600m.
- 코스별 시설·교통·주의는 GPX 지점 category(TRANS·TOILET·DANGER…) 그대로. 정류장 CSV·우리 문구 불필요.
- mountain-stats가 코스 요약을 주고, `?courseId=`로 지점 포함 상세를 준다(함수 6개 상한).
- 100대 명산 폴더에 우리 목록에 없는 4곳(남산=**경주 남산**·선운산·점봉산·대암산)이 있다. PEAK 좌표로 정상을
  채울 수 있다(대암산은 PEAK 없음). 데이터 백로그.
- 동네 명산 중 코스 없는 8곳: 남산(서울)·모락산·문수산·백운산(의왕)·봉산·북악산·불곡산·앵자봉.

### 코스 지도·주소·오늘 Pick (2026-09-07 밤)

- 코스 시트에 네이버 정적 지도 + 트랙 SVG. **호스트는 `maps.apigw.ntruss.com`** — 옛 `naveropenapi.apigw.ntruss.com`은
  같은 키로 401. raster-cors는 Referer 인증이라 Client ID가 번들에 들어가도 된다(콘솔에 등록한 URL에서만 동작).
  Client ID는 10자, Secret은 40자 — 처음에 둘이 바뀌어 들어가 401이 났다.
- 역지오코딩(`build-courses.mjs --geocode`)으로 635코스 들머리 주소를 `course-addresses.json`에 캐시.
  입구 이름 없는 코스는 "불광동 5.8km 코스". 시트에 주소 + "주소 복사"(SDK setClipboardText → navigator).
- 시설 카드는 코스 시트의 교통·주차·편의와 겹쳐서 뺐다. facilities 표 drop.
- 코스 시트 아이콘은 Tossface 이모지(`.tf`). 편의에서 REST·INFO 제외, 묶음당 8개 + 외 N곳.
- 쉐어링크: `sharelink-docs.toss.im/guide/open-api/*.md`가 문서. 토큰 oauth2.cert.toss.im client_credentials,
  베스트·카테고리 베스트(10,582개 카테고리 트리)·하루특가·상품 상세·링크 발급. 카테고리 베스트를 187개 연달아
  부르면 429가 난다. 일 사용 상한은 "응답 상품 수" 기준이라 한 번에 1,700개 받은 건 낭비였다 — 다음엔 리프 몇 개만.
  32개 큐레이션 → picks.json → 오늘 Pick 탭·추천 물건 카드 켜짐.
- 북한산 0053·0055는 서울 성곽·북악산 걷기(사직단→청운대, 부암동→북악팔각정). 9/8 `EXCLUDE_FILES`로 제외, 북한산 34코스.

### 오늘 Pick 매일 갱신·광고·홈 필터 (2026-09-08 새벽)

- 오늘 Pick 123개(장비 20·의류 16·음식 73·안전 14). 할인 배지는 50% 이상만(정가 부풀림, 중앙값 38%).
- 매일 갱신: `scripts/daily-picks.sh`(launchd 07:30) → `build-picks --write` → `docs/picks.json` push → 앱이
  시작할 때 GitHub Pages에서 받는다(`loadPicks`, 실패 시 번들 사본). 하루특가는 `dealEndsAt`으로 화면에서 빠진다.
  쉐어링크 API가 이 Mac IP만 허용해서 서버가 아니라 Mac에서 돈다. **launchd 설치는 사용자가**(plist 주석 참고).
- 전면 광고: 인증 성공 화면에서 preload, "내 스탬프"로 넘어갈 때 show, 닫히면 이동. 인증 버튼 앞에는 두지 않는다
  (정책: 서비스 목표 방해 금지, 광고=보상 연결 금지). ID는 `VITE_AD_GROUP_ID` — **테스트는 테스트용 ID 필수**
  (운영 ID로 테스트하면 제재). 비우면 광고 없음.
- 홈 토글을 "전체/100대/동네 · 거리순/안 모은 순"으로 줄여 375px 한 줄(279px).
- 광고 테스트 ID는 문서 고정값: 전면 `ait-ad-test-interstitial-id`, 배너 `ait-ad-test-banner-id`(리스트형).
  테스트 번들 .env에 이 값, 검수 번들엔 운영 ID(전면 `ait.v2.live.936076ea843347c9`, 배너는 콘솔에서 발급 필요).
- 배너: 산 상세 추천 물건 카드와 코스 카드 사이 한 곳(`AdBanner`, variant card). NoFill·실패면 자리를 접는다.
  TossAds.initialize는 앱에서 한 번(컴포넌트가 공유하는 전역 상태). 샌드박스는 광고 미지원 — 실기기 QR로만.

### 번들 23·24 (2026-09-08 00:10)

- 23 = 테스트 ID 번들(테스트 푸시만). 24 = 검수용: 전면 광고 운영 ID, 배너 없음(ID 미발급). 빌드는
  `VITE_AD_GROUP_ID=… VITE_AD_BANNER_ID= npm run build`로 .env를 덮어서 — .env엔 테스트 ID를 그대로 둔다.
- 24 검수 요청(REVIEWING). 주요 기능 3개도 함께: 가까운 산 찾기 / 내 스탬프(?tab=stamps) / 오늘 정상 인증하기(?verify=1).
  승인돼도 자동 출시 아님 — 콘솔 웹 '앱 출시 > 출시하기'는 사용자가 누른다. 앱정보(스크린샷·설명)도 별도 IN_REVIEW.
- 9/8 번들 24 검수는 CANCELED(사유 없음). 배너 운영 ID 받아 `npm run build:review`(.env의 AD_*_LIVE_ID로 덮음)로 번들 25 → REVIEWING → 취소. 배너 여백 수정(집계 카드 하단 24→12) 후 번들 26(테스트 ID) 테스트, **번들 27(운영 ID) 검수 요청 REVIEWING** (deploymentId 01a07ed6-0bab-7156-92c0-9d0713e59ba0). 승인 후 콘솔 웹에서 출시하기.
- 실기기 지도 안 뜸: SDK 3.1.1부터 origin이 `apps`/`private-apps`.tossmini.com 인데 NCP 웹 서비스 URL엔 `web`/`private-web`만 등록돼 있었다(curl Referer로 401 확인). 콘솔에 두 URL 추가로 해결(코드 변경 없음).
- review_cancel은 '검토 진행 중 취소 불가' 에러를 내지만 직후 목록엔 CANCELED로 찍혔다. 24도 요청 후 CANCELED가 됐던 것과 같은 현상 — 원인 미상.

### 프로모션·스마트 발송 셋업 (2026-09-08 오후)

- 프로모션 3개 RUNNING(총 30,000원, 10/31 종료): 정상 인증 10P/10,000원(01M1ZP54ATKFV8RR6PR9XF0WTX), 산행 시작 3P/5,000원(01M1ZP57ACGE87M8J3SA2P3NWN),
  첫 산행 30P/15,000원 혜택탭 "첫 산행 시작하기"(01M1ZP7REFRRF1DWACTXDYXM93). 1인 하루 한도 20/9/30원. 코드는 Supabase 시크릿 PROMO_SUMMIT/START/FIRST_CODE.
- 배운 것: 프로모션 예산은 줄일 수 없다(증액만). 처음 5만 원짜리를 만들었다가 종료(TERMINATED)하고 다시 만들었다 — 종료하면 미사용 예산이 월렛으로 돌아온다.
  혜택탭 미션 이름은 12자 이내. 생성 직후 자동 승인되는 경우가 많다. 시작 게이트는 TEST_ 코드 지급 1회(isTested).
- 지급 검증은 login GET `?promotion=<userKey>` 프로브(PROMO_PROBE_CODE의 TEST_ 코드만). 검증 후 시크릿 삭제. 인증서가 서버에만 있어 로컬에서는 못 부른다.
- 지급 코드: `_shared/toss.ts`(mTLS 클라이언트를 login에서 분리). start-hike는 시작 3P + 첫 산행(hike_starts 0건)이면 30P, verify-summit은 10P.
  응답 rewardP → 화면 문구. mountain-stats.rewards로 사전 고지("예산이 다 쓰이면 예고 없이 끝나요").
- 스마트 발송: 알림동의문 121085(SCHEDULED, 월·수·금·토) + 기능성 정기 푸시 `today-peak-hike-reminder`(07:00, "산행 알림 / 오늘 정상에서 산행 기록을 남길 수 있어요.") AI 검수 통과.
  앱은 내 스탬프 탭 카드에서 `Notification.requestAgreement`로 동의만 받는다(발송은 토스). 광고성 재방문 캠페인은 최근 30일 100명 이상부터, 콘솔 웹 전용.
- 남은 것: 포인트 문구·알림 카드가 들어간 번들(28)은 27 검수 결과 뒤에.
